# 로컬 전용 — 서버 접속/배포 기록(공인 IP·SSH 접속 정보 포함, 공개 저장소에 올리지 않음)
서버구축_배포_전과정_기록.md
서버구축_배포_전과정_기록.html

# 블로그 검사기 원본 소스(별도 프로젝트). 기능은 dashboard로 이식됨(BlogChecker.tsx/blog.ts/api).
# 자체 .git·node_modules·.env.local(비밀키)을 포함하므로 메인 저장소에서 제외.
daininc-naver-blog/

# SSH 인증키(서버 .pem) — 절대 커밋 금지(공개 저장소 유출 방지)
*.pem

# Claude Code 로컬 설정/세션(사용자별·로컬 전용 → 커밋 제외)
.claude/

# OS / 에디터
.DS_Store
Thumbs.db
